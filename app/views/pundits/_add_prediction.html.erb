<%= form_for(@prediction) do |f| %>
    <% if @prediction.errors.any? %>
	<div id="error_explanation">
	    <h2><%= pluralize(@pundit.errors.count, "error") %> prohibited this pundit from being saved:</h2>

	    <ul>
		<% @pundit.errors.full_messages.each do |message| %>
		    <li><%= message %></li>
		<% end %>
	    </ul>
	</div>
    <% end %>

    <div class="field">
	<div class="large-10 large-centered columns">
	    <%= f.text_area :content, placeholder: "Prediction" %>
	</div>

	<div class="large-10 large-centered columns">
	    <%= f.text_field :all_tags, placeholder: "Comma separated tag list" %>
	</div>

	<div class="large-10 large-centered columns">
	    <%= f.hidden_field :pundit_id, :value => @pundit.id %>
	</div>
    </div>
    <div class="actions">
	<%= f.submit %>
    </div>
<% end %>
