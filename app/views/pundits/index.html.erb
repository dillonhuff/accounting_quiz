<p id="notice"><%= notice %></p>

<h1>Pundits</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pundits.each do |pundit| %>
      <tr>
        <td><%= pundit.name %></td>
        <td><%= link_to 'Show', pundit %></td>
        <!-- <td><%= link_to 'Edit', edit_pundit_path(pundit) %></td> -->
        <!-- <td><%= link_to 'Destroy', pundit, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Pundit', new_pundit_path %>
