<%= form_for(@pundit) do |f| %>
  <% if @pundit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pundit.errors.count, "error") %> prohibited this pundit from being saved:</h2>

      <ul>
      <% @pundit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
